import { DivisibleDirective } from './divisible.directive';

describe('DivisibleDirective', () => {
  it('should create an instance', () => {
    const directive = new DivisibleDirective();
    expect(directive).toBeTruthy();
  });
});
